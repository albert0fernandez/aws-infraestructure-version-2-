# Copia este archivo a terraform.tfvars y rellena tus valores.
# El terraform.tfvars real NUNCA se sube a Git (está en .gitignore).

aws_region  = "eu-west-1"
admin_cidr  = "TU.IP.PUBLICA.AQUI/32" # sal.es/ip o curl ifconfig.me
# OJO: la contraseña de RDS NO puede llevar @ / " ni espacios
db_password = "UnaClaveFuerte-2026x"

# Opcional: key pair EC2 existente para entrar por SSH
# key_name = "mi-keypair"
