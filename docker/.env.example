# Copia este archivo a .env y cambia las contraseñas:  cp .env.example .env
# El .env real NUNCA se sube a Git
DB_ROOT_PASSWORD=cambiame-root-123
DB_NAME=pdo_solucion
DB_USER=userCRUD
DB_PASSWORD=CRUD@1234
